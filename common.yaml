version: "3.8"

services:
  frontend:
    container_name: frontend
    image: ghcr.io/jhonromero26/i2tec-frontend:latest
    restart: unless-stopped
    build: .
    networks:
      - nginx_default

networks:
  nginx_default:
    name: nginx_default
    external: true