---
type Props = {
  image?: string;
  size?: string;
  class?: string;
  alt: string;
};

const {
  image = "/images/user-placeholder.png",
  size,
  class: className,
  alt,
} = Astro.props;
---

<picture
  style={{
    width: size,
    height: size,
  }}
  class:list={[
    "block relative aspect-square overflow-hidden rounded-full",
    className,
  ]}
>
  <img
    loading="lazy"
    src={image}
    alt={alt}
    class="w-full h-full object-cover object-center"
  />
</picture>
